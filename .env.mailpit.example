# Mailpit - Local Email Testing
# Run Mailpit with Docker to catch emails locally:
#
#   docker compose -f docker-compose.mailpit.yml up -d
#
# Then access the mail UI at http://localhost:1080
# SMTP is available at localhost:1025
#
# For production, configure with your email provider:
#   MAIL_MAILER=smtp
#   MAIL_HOST=smtp.provider.com
#   MAIL_PORT=587
#   MAIL_USERNAME=your-username
#   MAIL_PASSWORD=your-password
#   MAIL_ENCRYPTION=tls
#   MAIL_FROM_ADDRESS="newsletter@yourdomain.com"
#   MAIL_FROM_NAME="${APP_NAME}"
