version: '3.8'

services:
  mailpit:
    image: axllent/mailpit:latest
    container_name: mailpit
    ports:
      - "8025:8025"
      - "1080:80"
    environment:
      - TZ=UTC
    restart: unless-stopped
